api:
  spoonacular:
    url: http://localhost:9999
spring:
  jpa:
    hibernate:
      ddl-auto: validate
  security:
    enabled: false

server:
  servlet:
    multipart:
      location: /src/test/resources/testing/uploaded